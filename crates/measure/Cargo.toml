[package]
publish = false  # git-only for now, not published to crates.io
name = "measure"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Ping and speed diagnostics over a bifrost stream, addressed by public key."
repository = "https://github.com/theia-hq/services"

[dependencies]
bifrost.workspace = true
futures.workspace = true
nauthy.workspace = true
thiserror.workspace = true
tightbeam-handler.workspace = true
tokio = { workspace = true, features = ["io-util", "macros", "sync", "time"] }
tracing.workspace = true

[dev-dependencies]
bifrost-mem.workspace = true
tokio = { workspace = true, features = ["rt-multi-thread", "test-util"] }

[lints]
workspace = true
