[package]
publish = false  # git-only for now, not published to crates.io
name = "transfer"
version = "0.1.4"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Receive a verified pushed file at a keyed node, off an admitted stream."
repository = "https://github.com/theia-hq/services"

[dependencies]
# theia
bifrost.workspace = true
tightbeam-handler.workspace = true

thiserror.workspace = true
tokio = { workspace = true, features = ["fs", "io-util", "macros"] }
tracing.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["rt-multi-thread"] }

[lints]
workspace = true
